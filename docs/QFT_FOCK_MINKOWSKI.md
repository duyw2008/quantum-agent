# QFT：闵可夫斯基空间与 Fock 空间的缝合

> 量子场论如何将时空连续统与粒子数态空间统一

---

## 目录

1. [核心问题](#1-核心问题)
2. [第一步：闵氏空间 → Fourier 模式分解](#2-第一步闵氏空间--fourier-模式分解)
3. [第二步：每个模式 → 量子升降算符](#3-第二步每个模式--量子升降算符)
4. [第三步：直积 → Fock 空间](#4-第三步直积--fock-空间)
5. [第四步：缝合——场算符 φ̂(x)](#5-第四步缝合--场算符-φx)
6. [物理后果](#6-物理后果)
7. [图示总结](#7-图示总结)
8. [在 Quantum Agent 中的验证](#8-在-quantum-agent-中的验证)

---

## 1. 核心问题

经典场论：场 `φ(x,t)` 是一个时空函数，值在每点确定。

量子场论：场变成算符 `φ̂(x)`，作用在 Fock 空间上。粒子是场的量子激发。

**关键缝合**：闵氏坐标 `(x,t)` 是场算符的参数，Fock 空间的态描述"这个模式有几个粒子"。

```
闵可夫斯基空间 M⁴  ──→  场算符 φ̂(x)  ──→  Fock 空间 F
 (x, t)           (x是坐标参数)       {|n₁, n₂, ...⟩}
```

---

## 2. 第一步：闵氏空间 → Fourier 模式分解

经典实标量场 `φ(x,t)` 满足 Klein-Gordon 方程 `(□ + m²)φ = 0`。Fourier 按平面波展开：

```
φ(x,t) = ∫ d³k/(2π)³ · 1/√(2ω_k) · [a(k) e^{-ikx} + a*(k) e^{+ikx}]

其中: kx = ω_k t - k·x,   ω_k = √(k² + m²)
```

**关键**：每个波矢 **k** 对应一个独立的振动模式——一个经典谐振子，频率 `ω_k`。

> 一个场 = 无穷多个独立谐振子的集合

闵氏空间的连续性被离散化为可数的模式标签。动量 `k` 成为模式的"名字"。

---

## 3. 第二步：每个模式 → 量子升降算符

对每个 k 模式做正则量子化（`[φ̂, π̂] = iħ`），经典振幅提升为算符：

```
a(k)  →  â(k)     湮灭算符
a*(k) →  â†(k)    产生算符

对易关系: [â(k), â†(k')] = (2π)³ δ³(k - k')
```

每个 k 模式现在是一个量子谐振子，有自己的 Fock 空间：

```
|n_k⟩  =  第 k 模式有 n_k 个量子激发

â(k)|n_k⟩   = √n_k · |n_k - 1⟩
â†(k)|n_k⟩  = √(n_k + 1) · |n_k + 1⟩
```

**物理意义**：
- `â†(k)` 产生一个动量为 `k`、能量为 `ω_k` 的粒子
- `â(k)` 湮灭一个这样的粒子
- 粒子是场的量子激发——不是独立的实体

---

## 4. 第三步：直积 → Fock 空间

所有 k 模式的 Fock 空间做张量积：

```
F = ⨂_k  F_k

基矢:  |n₁, n₂, n₃, ...⟩   =  |n₁⟩ ⊗ |n₂⟩ ⊗ |n₃⟩ ⊗ ...
```

不同 k 模式上的粒子数构成 Fock 空间的完整态。

| 态 | 物理意义 |
|---|---|
| `|0⟩` = `|0, 0, 0, ...⟩` | 真空——所有模式基态 |
| `â†(k₁)|0⟩` = `|1, 0, 0, ...⟩` | 一个动量为 k₁ 的粒子 |
| `â†(k₁)â†(k₂)|0⟩` | 两个粒子，动量 k₁ 和 k₂ |
| `(â†(k))^n/√(n!)|0⟩` | n 个动量相同的玻色子 |

**粒子数算符**：

```
N̂ = ∫ d³k · â†(k)â(k)      总粒子数（所有模式求和）
Ĥ = ∫ d³k · ω_k · â†(k)â(k)  总能量
```

---

## 5. 第四步：缝合——场算符 φ̂(x)

场算符将两者统一：

```
φ̂(x) = ∫ d³k/(2π)³ · 1/√(2ω_k) · [â(k) e^{-ikx} + â†(k) e^{+ikx}]
```

- **x = (t, x)** 是闵氏坐标——算符的**参数**
- **â(k), â†(k)** 作用在 Fock 空间——算符的**算符部分**

φ̂(x) 就是缝合线。

**场算符的物理效果**：

```
φ̂(x)|0⟩          粒子在时空点 x 产生（自真空涨落）
⟨0|φ̂(x)φ̂(y)|0⟩    粒子从 x 传播到 y（Feynman 传播子）
⟨k₁k₂|φ̂(x)|0⟩    在 x 点可以"找到"这两个粒子
```

闵氏距离 `(x-y)²` 直接出现在传播子分母中——时空结构编码在 Fock 空间的矩阵元里。

---

## 6. 物理后果

### 6.1 粒子 = 场的量子激发

不是"粒子穿行于时空"，而是"场在 Fock 空间中的激发在闵氏空间中表现为粒子"。

### 6.2 真空不空

真空 `|0⟩` 是 Fock 空间的一个态——所有模式的基态。由于零点能 `ħω/2`，真空有非零的场涨落：

```
⟨0|φ̂(x)²|0⟩ ≠ 0
```

Casimir 效应、Lamb 移位都由此而来。

### 6.3 因果关系在 Fock 空间中编码

类空间隔 `(x-y)² > 0` 时，`[φ̂(x), φ̂(y)] = 0`——场算符的对易子在类空间隔上为零，保证了微观因果性。这是闵氏几何通过场算符对易子映射到 Fock 空间的结果。

---

## 7. 图示总结

```
闵可夫斯基空间 M⁴                       Fock 空间 F
                                              │
经典场 φ(x,t)  ──Fourier──→  {k 模式}  ──量子化──→  {â(k), â†(k)}
                                              │
                                              ├──→  |n_k⟩  (单模式 Fock 态)
                                              │
                                              └──→  |n₁, n₂, ...⟩  (全 Fock 空间)
                                                    ↑
场算符 φ̂(x)  ←────────── 缝合 ──────────────┘
(参数: x∈M⁴)      ã(k)e^{-ikx} + h.c.
```

| 概念 | 闵氏侧 | Fock 侧 |
|------|:------:|:------:|
| 基本对象 | 时空坐标 x = (t, x) | 粒子数态 \|n₁, n₂, ...⟩ |
| 动力学量 | k (波矢/动量) | n_k (占据数) |
| 观测 | φ̂(x)——在 x 处测量场 | ⟨N̂⟩——统计粒子数 |
| 传播 | 传播子 ⟨φ̂(x)φ̂(y)⟩ | 产生→湮灭振幅 |
| 因果性 | (x-y)² > 0 → 类空间隔 | [φ̂(x), φ̂(y)] = 0 |
| 真空 | — | \|0⟩ (基点) |

> **闵氏空间告诉你"在哪里"，Fock 空间告诉你"有多少"。**
> **场算符 φ̂(x) 同时理解两种语言。**

---

## 8. 在 Quantum Agent 中的验证

### 8.1 标量场对易子

```python
from src.qft import ScalarField

sf = ScalarField(mass=1.0)

# 验证等时对易子: [φ̂(x), π̂(y)] = i δ(x-y)
sf.commutator(0, 0)      # 近奇异的 δ 函数近似
```

### 8.2 格点 φ⁴ 理论的 Fock 空间

```python
from src.qft import LatticePhi4

# 一维格点，每个格点是一个"模式"
lat = LatticePhi4(N_sites=10, mass=1.0)

# 精确对角化找到基态
E0, psi0 = lat.ground_state()
E0          # 基态能量 (真空)
```

### 8.3 传播子和 Feynman 振幅

```python
from src.qft import feynman_amplitude_phi4_2to2

# φ⁴ 理论中 2→2 散射的树图振幅
amp = feynman_amplitude_phi4_2to2(s=10, t=-2, u=-8, coupling=1.0)
# 分母含 (s-m²)——闵氏不变量直接出现在振幅里
```

---

## 参考文献

- Peskin & Schroeder, *An Introduction to Quantum Field Theory*, Ch. 2
- Weinberg, *The Quantum Theory of Fields*, Vol. 1, Ch. 4
- Quantum Agent 文档: `docs/PHYSICS.md`
